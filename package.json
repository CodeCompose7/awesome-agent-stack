{
  "name": "awesome-ai-stack-workspace",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=20.3.0"
  },
  "scripts": {
    "dev": "pnpm --filter awesome-ai-stack dev",
    "build": "pnpm --filter awesome-ai-stack build",
    "preview": "pnpm --filter awesome-ai-stack preview",
    "check": "pnpm -r check",
    "build:all": "pnpm -r build"
  }
}
